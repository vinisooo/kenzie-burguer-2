import styled from "styled-components";

export const StyledCta = styled.p`

    text-align: center;
    font-weight: 400;
    font-size: 14px;
    line-height: 18px;

    color: var(--color-grey-3);

    max-width: 70%;
    margin: 0 auto;
`